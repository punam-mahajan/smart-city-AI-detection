# AI-Based Smart City Problem Detection System

This project detects smart city issues like potholes, garbage, and clean roads using an AI model.

Technologies used:
- Python
- Flask
- Machine Learning
- HTML, CSS, JavaScript

The system allows users to upload an image and predicts the type of problem. The backend processes the image and returns the result to the frontend.
